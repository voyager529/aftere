# =============================================================================
# after-e- blueprint — Vaultwarden OIDC (built-in SSO)
# =============================================================================
# Uses Vaultwarden's native SSO (1.32+). The callback is /identity/connect/
# oidc-signin on the vault subdomain. Vaultwarden SSO also requires server-side
# env (SSO_ENABLED=true, SSO_AUTHORITY, SSO_CLIENT_ID/SECRET) — that's the
# compose/postinstall side; this blueprint is the Authentik side. VERIFY the
# callback path against your Vaultwarden version's SSO docs.
# =============================================================================
version: 1
metadata:
  name: aftere-app-vaultwarden
  labels:
    blueprints.goauthentik.io/instantiate: "true"
entries:
  - model: authentik_providers_oauth2.oauth2provider
    id: aftere-oidc-vaultwarden
    state: present
    identifiers:
      name: aftere-vaultwarden
    attrs:
      client_type: confidential
      client_id: vaultwarden
      client_secret: !Env OIDC_VAULTWARDEN_SECRET
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-explicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      sub_mode: hashed_user_id
      issuer_mode: per_provider
      include_claims_in_id_token: true
      access_code_validity: "minutes=1"
      access_token_validity: "hours=1"
      refresh_token_validity: "days=30"
      redirect_uris:
        - url: !Format ["https://vault.%s/identity/connect/oidc-signin", !Env AFTERE_DOMAIN]
          matching_mode: strict
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, "goauthentik.io/providers/oauth2/scope-openid"]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, "goauthentik.io/providers/oauth2/scope-profile"]]
        - !Find [authentik_providers_oauth2.scopemapping, [name, "aftere email (verified=true)"]]

  - model: authentik_core.application
    id: aftere-app-vaultwarden
    state: present
    identifiers:
      slug: vaultwarden
    attrs:
      name: Vaultwarden
      provider: !KeyOf aftere-oidc-vaultwarden
      policy_engine_mode: any
      meta_launch_url: !Format ["https://vault.%s", !Env AFTERE_DOMAIN]
